// "Use Strict"

// // data storage
// const rootElement = document.documentElement;

// // navbarOnScroll gets changed

// let navigationBar = document.querySelector('nav');

// document.addEventListener('scroll', navbarScrollFunction);

// function navbarScrollFunction() {
//     let scrollTotal = rootElement.scrollHeight - rootElement.clientHeight;

//     if((rootElement.scrollTop / scrollTotal) > 0.0) {
//         // change navigation bar background color
//         navigationBar.classList.add('bg-white');
//     } else {
//         navigationBar.classList.remove('bg-white');
//     }
// }
